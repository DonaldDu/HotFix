buildscript {
    ext.kotlin_version = '1.3.71'
    ext.libV = '1.0.0'
    ext.pluginEnableFlag = true//fixme comment it when publish
    ext.pluginEnable = project.hasProperty('pluginEnableFlag')
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
    dependencies {
        classpath 'com.novoda:bintray-release:0.9.2'
        classpath 'com.android.tools.build:gradle:3.6.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        if (pluginEnable) classpath "com.github.DonaldDu.HotFix:Plugin:$libV"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://jitpack.io' }
    }
    tasks.withType(Javadoc) {
        enabled = false
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}